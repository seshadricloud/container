# ============================================================================
# Kubernetes Secrets (Database Credentials)
# ============================================================================

apiVersion: v1
kind: Secret
metadata:
  name: database-credentials
type: Opaque
stringData:
  username: postgres
  password: "REPLACE_WITH_ACTUAL_PASSWORD"
  host: "myapp-dev-db.c9akciq32.us-east-1.rds.amazonaws.com"
  port: "5432"
  database: appdb
  connection-string: "postgresql://postgres:REPLACE_WITH_PASSWORD@myapp-dev-db.c9akciq32.us-east-1.rds.amazonaws.com:5432/appdb"
